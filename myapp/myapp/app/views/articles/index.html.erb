<h1>Listing articles</h1>
 
<table>
  <tr>
    <th>Name</th>
    <th>Descryption</th>
  </tr>
 
  <% @articles.each do |article| %>
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'New Ticket', new_article_path %>